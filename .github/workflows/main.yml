name: Running MATLAB tests
run-name: Running MATLAB tests
on: 
  pull_request:
    branches: [ "main", "develop"]
jobs:
  test-matlab:
    runs-on: self-hosted 
    steps:
      - uses: actions/checkout@v3
      - name: Run matlab tests
        run: ./run_tests_matlab.sh
